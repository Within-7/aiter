/**
 * Git-related components
 *
 * - GitView: Main Git panel view (branches, commits, changes)
 * - GitHistoryPanel: Git commit history display
 */

export { GitView } from './GitView'
export { GitHistoryPanel } from './GitHistoryPanel'
